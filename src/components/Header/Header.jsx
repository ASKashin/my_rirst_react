import React from "react";

const Header = (props) => {

    return (
        // <h1>{props.title}</h1>
        <p>{props.title}</p>
    )
};
// const Parag = (props) => {
//
//     return (
//         // <h1>{props.title}</h1>
//         <p>{props.title}</p>
//     )
// };


export default Header;